# Session Log — 2026-05-20

## What Was Done

- Initialised CLAUDE.md for the project
- Created `agent_workspace/` as a git-tracked working directory (separated from main codebase)
- Set up project management structure: `planning/`, `tasks/`, `sessions/`
- Set up compound-engineering structure for technical specs and change proposals
- Seeded `tasks/TASKS.md` with project phases and candidate code tasks from existing analysis
- Stub planning docs created referencing existing `code_analysis/` baseline documents

## Decisions Made

- Use `agent_workspace/` for project management (task tracking, session logs, planning docs)
- Use compound-engineering skill for technical specs and change proposals
- `TASKS.md` is the source of truth for work state; compound-engineering documents link from task entries
- No `tasks/specs/` in agent_workspace — compound-engineering handles that role

---

## Session 2 (continued same day)

### What Was Done

- Completed Phase 1 Architecture Review (TASK-001):
  - Ran parallel code exploration agents on all five review questions
  - Closed factual gaps with targeted grep/read checks
  - Stress-tested two key findings (event-loop coupling, variant-caller divergence)
  - Filled `planning/architecture_review.md` with full Findings + Conclusions sections
- Discovered Phase 2 Code Review (TASK-002) was already complete — `planning/code_review.md` was fully populated with 8 cross-cutting findings, 5 confirmed bugs, per-module analysis, and tooling recommendations
- Updated `tasks/TASKS.md`: TASK-001 and TASK-002 marked Done; TASK-003 (Phase 3) moved to In Progress; added TASK-029–033 (architecture-derived tasks)
- Fixed all cross-references from `code_analysis/` to `archive/code_analysis/` across planning docs and this workspace README
- Updated `agent_workspace/README.md` phase status

### Key Findings (architecture review)

1. **GUI/logic coupling** — Severe. 303 widget references in `performAssembly()`. Headless achievable via callback refactor (no Qt event-loop dependency in pipeline).
2. **Inter-module contracts** — Entirely implicit; no pipeline handoff between modules — users must manually supply files to each module.
3. **Utility scripts** — 3 of 18 are Class B (quick `if __name__` fix); 15 are Class C (need function extraction).
4. **State management** — Filesystem-based (sound design); `os.chdir()` is a constraint for any future in-process consolidation.
5. **Dual UI** — `assembly.py` is orphaned dead code referencing two non-existent utility scripts. `assemblyQt.bak` also committed. bcftools/lofreq calls in assemblyQt.py are all commented out — variant calling is identical between versions (VarScan).

### Current State

Phases 1 and 2 complete. Phase 3 (prioritised list of changes, TASK-003) is now In Progress.

### Next Session

- Begin Phase 3: produce a prioritised list of changes from the combined Phases 1 + 2 findings
- Input: `planning/architecture_review.md`, `planning/code_review.md`, `tasks/TASKS.md` (TASK-010 through TASK-033)
- Output: ranked list of changes by impact vs effort, feeding into Phase 4 (IK/RS decisions)

---

## Session 3 (continued same day)

### What Was Done

- Executed Phase 2 code review (TASK-002) in full:
  - Ran parallel exploration agents across all 20+ Python source files
  - Bootstrapped `pip` into `~/.local/bin/` via `get-pip.py` (system Python 3.14 had no pip)
  - Installed Ruff 0.15.13 and Bandit 1.9.4 via `pip install --user`
  - Ran `ruff check src/` → **436 violations** across all modules
  - Ran `bandit -r src/ -f txt` → **916 high-confidence security findings** (579 definite shell injection, 167 partial-path)
  - Read remaining uncovered files: `dbsubmissionQt.py`, `lncRNA_annotation.py`, `varscanFilter.py`, `getBestAssembly.py`, `polyAn.py`, `plotHeatmap.py`, `changeHeaderFormat.py`
  - Wrote full `planning/code_review.md` (replacing stub): executive summary, 8 cross-cutting findings, 5 confirmed bugs, per-module analysis for all 20 files, tooling recommendations
  - Created `pyproject.toml` with Ruff config and `.bandit` config at project root
  - Added TASK-021 through TASK-028 to `TASKS.md` (hotfixes + new tasks from review)
- Updated project documentation: `CLAUDE.md` (linter section), memory files (`project_overview.md`, `project_structure.md`)

### Key Findings (code review)

1. **579 confirmed shell injection sites** — `os.system()` with unsanitised user input throughout all modules; any path with a space will break the pipeline
2. **BUG-1 (Critical):** `toPlainText` missing `()` in 5 modules — file-selection guard never fires
3. **BUG-2 (Critical):** Copy-paste error in `completeGenome.py:105` — writes `firstPortion` instead of `lastPortion`, silently producing wrong genome sequence
4. **BUG-3 (High):** Hardcoded `16` instead of `kmerLength` in `genotypingQt.py:511`
5. **Debug code in production:** `sys.stdin.read(1)` blocks GUI in `dbsubmissionQt.py` (lines 521, 546); debug prints throughout `polyAn.py`
6. **Performance bug:** `makeblastdb` rebuilt inside loop per-lncRNA in `lncRNA_annotation.py` — should be called once
7. **Deprecated API:** `Seq.reverse_complement()` module-level call in `varscanFilter.py` — breaks on newer BioPython

### Current State

Phase 2 complete. Phase 3 (TASK-003) in progress.

### Next Session

- Begin Phase 3: produce prioritised list of changes
- Consider tackling the one-line hotfixes (TASK-021, TASK-022, TASK-023) first — they are independent of Phase 3 and fix confirmed bugs

---

## Session 4 (continued same day)

### What Was Done

- Completed Phase 3 (TASK-003): produced `planning/priority_plan.md`
  - First pass built from `planning/code_review.md` alone
  - Second pass refined by cross-referencing `planning/architecture_review.md` — four items changed (see below)
- Created `planning/executive_summary.md` — handover document covering project management infrastructure and technical findings across all phases; suitable for passing to RS or a new contributor
- Created `agent_workspace/md_to_docx.py` — Python script converting all planning markdown files to professionally styled Word documents (navy/steel-blue theme, Calibri body, Courier New code blocks, table styling)
- Generated Word documents in `agent_workspace/output/`:
  - `Architecture_Review.docx`
  - `Code_Review.docx`
  - `Priority_Plan.docx`
  - `Executive_Summary.docx`
- Resolved TASK-033 scope: architecture review confirmed `assembly.py` + `assemblyQt.bak` can be deleted without investigation — updated task description in TASKS.md
- Updated TASKS.md: TASK-003 moved to Done; TASK-004 moved to In Progress; candidate task note updated to reflect prioritisation is complete
- Updated `agent_workspace/README.md`: phase status, key documents table

### What Changed Between Priority Plan v1 (code-review-only) and v2 (both reviews)

| Change | Source | Effect |
|--------|--------|--------|
| `assembly.py` + `assemblyQt.bak` confirmed deletable without investigation | Arch review Q5 | Moved from "defer/investigate" to Tier 3 |
| Class-B utils (`scaffold_builder.py` etc.) are stepping-stone before subprocess migration | Arch review Q3 | New Tier 4a before full subprocess migration |
| Inter-module contracts must be documented before API-changing refactors | Arch review Q2 | New Tier 6 gate before God-method extraction |
| `os.chdir()` must be fixed in same pass as `performAssembly()` extraction | Arch review Q1+Q4 | Adds scope to Tier 7; cannot split the two tasks |

### Current State

Phases 1, 2, 3 complete. All analysis and planning done. No code changes made yet.
Phase 4 (IK/RS decisions) is next — IK and RS should review `output/Priority_Plan.docx` together and populate `planning/decisions.md`.

### Next Session

- Phase 4: IK/RS decisions session — work through `planning/priority_plan.md` tier by tier, record decisions in `planning/decisions.md`
- After Phase 4: begin Phase 5 implementation, starting with Tier 1 one-line bug fixes (TASK-021, TASK-022, TASK-023)
