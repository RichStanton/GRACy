# Journal — 2026-07-09

First working session in the WSL clone (`~/projects/GRACy`), now the single home. Focus: finish the
Windows→WSL consolidation, settle the delivery method, and stand up the backlog.

## What was done

- **Git consolidation.** Normalised line endings to LF (`core.autocrlf false` + `.gitattributes`),
  clearing ~256 files of CRLF noise from the Windows→WSL move. Restored the fuller `README.md` and the
  improved `ARCHITECTURE.md`; added tooling (`pyproject.toml`, `.bandit`) and the `operations/` plan.
  Committed as `840e6ee` and pushed to `origin/master`.
- **Standardised on the Matt Pocock skills.** Removed `openspec/` (the retired OpenSpec process); the
  Compound Engineering plugin stays disabled. `CLAUDE.md` + `operations/` + `docs/agents/` are the KB.
- **Established the push workflow.** The user's interactive WSL terminal `git` resolves to Windows
  git + Windows Credential Manager and authenticates as `ikeddie` (no write access → 403). The account
  with write access is `developer-keystrand`; its PAT is stored for Linux `git` (`credential.helper
  store`). **Pushes now go through Claude's Linux git, not the user's terminal.**
- **Filed the backlog.** Turned the improvement-plan (11 items) + the 5 confirmed bugs into
  **16 GitHub issues (#2–#17)** on `RichStanton/GRACy`, labelled `ready-for-agent` / `ready-for-human`,
  with dependency links. In-repo index at `operations/delivery/backlog.md`.
- **Launched the toolchain build.** `install.sh` running in the background (bundled Miniconda +
  ~25 bioconda tools). Needed to *run/verify* the pipeline; not needed to *edit* the bug fixes.

## Decisions

- **Delivery = one issue → one PR**, Claude builds and verifies, user owns merges. Code changes go
  through branches/PRs; only housekeeping (this consolidation, docs) goes straight to `master`.
- **GitHub Issues is the tracker** (not files in git); `operations/delivery/backlog.md` is the
  git-tracked mirror so progress is visible in the repo too.
- **Verification is two-tier.** Logic bugs (BUG-2/3/4/5) get isolated tests now; BUG-1 gets a headless
  PyQt5 test; true end-to-end scientific correctness waits on the toolchain + a golden dataset (#11).

## Next

- **BUG-2 (#3)** — highest-impact, first through the full loop: branch → fix → isolated test → PR.
- Then **BUG-3 (#4)** and **BUG-5 (#6)** (logic bugs verifiable now), **BUG-1 (#2)** once PyQt5 is
  available, **BUG-4 (#5)** via Ruff.
- Once `install.sh` finishes: smoke test (#11) to unlock end-to-end verification for everything after.

## Blockers / notes

- Terminal `git push` from the user fails (auth as `ikeddie`); Claude pushes from its side.
- Exposed `developer-keystrand` PAT in chat earlier — **should be rotated** and `~/.git-credentials`
  updated.

## Update — later, same day

- **Toolchain finished** — `install.sh` completed (conda py3.7.4 + conda2, SPAdes/BWA/Bowtie2/
  Samtools/BLAST/LoFreq, Biopython 1.76, PyQt5). End-to-end verification now possible.
- **BUG-2 (#3) reclassified Critical → Low.** Traced the data flow: `tempFasta.fasta`/`lastPortion`
  at the buggy line are **dead** (never read); the reconstructed genome (`newGenome2.fasta`) is
  independent of it. Real impact is nil — the "silent wrong genome" framing was overstated. Finding
  posted to #3; deprioritised as tidy-up (delete the dead block, ideally behind the smoke test).
- **BUG-1 (#2) fixed and PR'd.** `toPlainText` → `toPlainText()` in all **6 live** Qt modules
  (grep found `annotationQt.py` too, not in the original review list; dead twins left for #7).
  Verified: all 6 compile on py3.7.4 + a headless PyQt5 test proves the guard fires when empty,
  passes when populated, and the old form never fired. Branch `fix/toplaintext-guard` → **PR #18**
  (Closes #2), awaiting review/merge.
- **Lesson reinforced:** trace impact before trusting a review's severity, and grep for real sites
  rather than trusting listed line numbers.

## Session wrap-up

- **Tests are now committed.** Added a `tests/` harness (stdlib `unittest`, the repo had none) with
  regression coverage for the BUG-1 guard — source-level (all 6 modules) + a headless PyQt5 behaviour
  test. Proven to fail on a reintroduced bug. Folded into **PR #18**.
- **Delivery docs clarified.** Realised BUG-1 was done *fix-first*, not *test-first* — against the
  documented `/tdd` step. Fixed the docs so it won't recur: `operations/delivery/README.md` is now the
  single detailed home (pipeline + an explicit test-first red→green section); `CLAUDE.md` trimmed to a
  map with a one-line "test-first (`/tdd`), not fix-first" flag pointing there.
- **End state:** `master` @ `1d828f4`, level with origin. **PR #18** (BUG-1 fix + tests) open, awaiting
  your review/merge. Backlog #2–#17 filed; toolchain installed and working.

### Next session
- **BUG-3 (#4)** via `/tdd`, properly: plan behaviours → RED (failing test) → GREEN (fix) → PR. Then
  **BUG-5 (#6)**. **BUG-2 (#3)** is now a low-priority tidy (dead code). **#11** (smoke test) unlocks
  end-to-end verification for the structural items.

### Outstanding
- **Rotate the exposed `developer-keystrand` PAT** and update `~/.git-credentials`.
- Working tree carries the install-regenerated `GRACy.py` (build artifact, intentionally uncommitted —
  see #9) plus transient install/test byproducts (`installation.log`, `__pycache__/`) — hygiene is #8.

## Update — second session (AFK bug-hotfix batch)

Merged BUG-1 and cleared the rest of the confirmed-bug layer test-first. `gh` was also wired up
(token in `~/.config/gh/hosts.yml`; scopes `repo`,`project` — no `read:org`, so `gh auth login`
is bypassed by writing the config directly). User authorised the AFK scope: **bugs only, up to the
`ready-for-human` gate**; one PR per bug; Claude does not self-merge (see the
`afk-delivery-preferences` memory).

- **BUG-1 (#2)** — **merged** (squash) as [PR #18]; `master` @ `d7aca8a` carries the `toPlainText()`
  fix in all 6 live modules + the `tests/` harness. Merge was the user's explicit call.
- **BUG-3 (#4)** — [PR #19]. The dedupFile2 k-mer loader hardcoded 17-mers (`range(0,len-16)` +
  `[a:a+17]`) while dedupFile1 used `kmerLength`; counts drifted silently for any k≠17. Extracted a
  shared `collectKmers(sequence, kmerLength)` helper used by both loops. Behaviour-identical in prod
  (jellyfish counts 17-mers). Tests: helper behaviour + source guard.
- **BUG-5 (#6)** — [PR #20]. Hardcoded HCMV genome size `235646.0` (×2) → single `HCMV_GENOME_SIZE`
  constant. Pure DRY; coverage numbers unchanged (test pins the value). 
- **BUG-4 (#5)** — [PR #21]. `warnings` is a function-local (no `import warnings`) assigned *after*
  the loops that append to it → `UnboundLocalError` on the `line is None` path, in two blocks.
  Initialise once at the top of the `for f in prot2map` loop; removed the two misplaced late inits.
  Test: stdlib-only AST check that first-assign precedes first-use (no conda needed).

**Method note:** all three delivered RED→GREEN via `/tdd`. Two review-worthy judgement calls, both in
the PR bodies: (a) BUG-3 was deeper than the issue said (the slice length `17` was hardcoded too, not
just the `16`); (b) `warnings` (BUG-4) is an append-only accumulator nothing reads, so the fix only
removes the crash path — no output change.

### Next session (updated)
- Confirmed-bug layer is **done** — PRs #19/#20/#21 await review/merge (user owns merges).
- Resume Block 1 at **#11 (smoke test)** — the keystone that unlocks the structural phase (#12–#17).
  Note #11 needs a maintainer-blessed golden output for true regression power; smoke+sanity can land
  first. **#12+ are `ready-for-human`** — not for AFK.
- Housekeeping items #7/#8/#9/#10 (`ready-for-agent`) remain; user prefers these **grouped** into
  one or two PRs rather than one-per-issue.
